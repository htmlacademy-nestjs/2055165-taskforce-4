export enum TITLE_LENGTH {
  MIN = 20,
  MAX = 50
}

export enum DESCRIPTION_LENGTH {
  MIN = 100,
  MAX = 1024
}

export enum ADRESS_LENGTH {
  MIN = 10,
  MAX = 255
}

export enum PRICE_VALUE {
  MIN = 0
}

export enum TAGS_COUNT {
  MIN = 1,
  MAX = 5
}

export enum TAG_LENGTH {
  MIN = 3,
  MAX = 10
}
